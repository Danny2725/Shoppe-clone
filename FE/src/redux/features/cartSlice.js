import { createSlice } from "@reduxjs/toolkit";

export const cartSlice = createSlice({
  name: "cart",
  initialState: {
    // Khai báo initialState cho cartSlice nếu cần
  },
  reducers: {},
});

// Các selectors nếu cần

export default cartSlice.reducer;
