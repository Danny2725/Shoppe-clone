import { createSlice } from "@reduxjs/toolkit";

export const imgSlice = createSlice({
  name: "bgImgs",
  initialState: {
    // Khai báo initialState cho imgSlice nếu cần
  },
  reducers: {},
});

// Các selectors nếu cần

export default imgSlice.reducer;
